# dsh-provider-usage 插件设计方案

> DSH 插件 · 供应商「用量 / 余额 / 套餐」查询
> 版本：0.1（方案稿） · 状态：待评审 · 日期：2026-08-23

---

## 1. 背景与目标

### 1.1 背景

- **参考形态**：`dsh-pet`（@linxin666/dsh-pet）——注册表驱动、Host/Client 双半区、
  回环路由、设置分区、TS 源码 + 测试、`dsh.bundle.patch` 自动装配，是这个方案的工程范本。
- **参考功能**：CC Switch（farion1231/cc-switch）的「供应商用量/余额/套餐查询」——
  多供应商、多套餐卡片、余额/官方订阅/Coding Plan/Copilot/自定义脚本多路查询。
- **现状缺口**：DSH 生态里只有针对 OpenCode Go 单一供应商的用量插件
  （`dsh-opencode-go-box` / `dsh-balance`），缺少通用的、可扩展的多供应商用量查询能力。

### 1.2 目标

做一个 DSH Web GUI 插件，让用户在 dsh 里直接查看任意已配置供应商的：

1. **账户余额**（DeepSeek / OpenRouter / SiliconFlow / StepFun / Novita 等有余额接口的平台）；
2. **订阅套餐额度**（Claude / Codex / Gemini / OpenCode Go 等以百分比或额度窗口暴露的套餐）；
3. **Coding Plan / 包月套餐**（火山方舟 Agent Plan、智谱 Coding Plan、Kimi Coding、ZenMux 等）；
4. **自定义供应商查询**——通过**声明式查询模板**接入任何提供 usage/balance 接口的服务。

### 1.3 非目标（一期不做）

- ❌ 成本/Token 统计仪表盘（依赖"流量经过本地代理"或 DSH 会话日志，另立项）；
- ❌ 执行**任意 JavaScript** 的用户脚本（安全面大，二期评估，见 §6.4）；
- ❌ 供应商配置的"切换/管理"（那是 CC Switch 的本职，DSH 已有自己的模型设置）。

---

## 2. 总体架构

```
┌─ dsh web profile（Host 进程，Node）───────────────────────────────┐
│  dsh-provider-usage                                                │
│   ├─ registry.ts       供应商/查询模板注册表（内置 + 用户目录 + 注入） │
│   ├─ adapters/         内置原生适配器（balance/subscription/plan/go）│
│   ├─ engine.ts         查询引擎：模板分派 + 并发池 + 超时 + 重试      │
│   ├─ credentials.ts    凭证解析（DSH 凭证层，不回传明文）             │
│   ├─ cache.ts          快照缓存（stale-while-error）+ 诊断快照环      │
│   ├─ routes.ts         /api/provider-usage/*（仅回环）               │
│   └─ index.ts          cordis 插件入口（settings 命名空间 + 装配）    │
├─ 浏览器（web GUI）──────────────────────────────────────────────────┤
│  lib/client.js（或 client 半区 bundle）                              │
│   ├─ settings.section 「供应商用量」：供应商卡片 + 套餐卡片 + 模板管理   │
│   ├─ sidebar.footer.action 紧凑徽标（当前主要供应商摘要）             │
│   └─ conversation.composer.dock 可选：底部常驻摘要                   │
└─────────────────────────────────────────────────────────────────────┘
```

- **数据流**：浏览器 → 同源 `/api/provider-usage/*`（fetch）→ Host 引擎 → 供应商接口；
  **一切对外请求都发生在 Host 进程**，浏览器只拿结构化结果，key 不出 Host。
- **与 dsh-pet 一致的点**：注册表驱动、回环路由防护、设置分区、生命周期清理、双语。
- **与 cc-switch 一致的点**：模板类型分派、套餐卡片多返回、失败语义区分
  （确定性失败 vs 瞬时失败）、缓存与"保留上次成功值"。

---

## 3. 包结构（源码态，构建产物为 lib/）

```
dsh-provider-usage/
├── package.json              # name: dsh-provider-usage；dsh.client / dsh.bundle 声明
├── cordis.patch.yml          # bundle patch：插入插件行（自动 reconcile）
├── tsconfig.json             # tsc -b 严格模式
├── tsdown.config.ts          # 构建 host + client 双产物
├── vitest.config.ts
├── contracts/
│   ├── query-template.schema.json   # 声明式查询模板 JSON Schema（权威校验）
│   └── provider-usage.schema.json   # 查询结果契约
├── src/                      # Host 半区（TS）
│   ├── index.ts              # 插件入口：apply/inject/name/Config
│   ├── registry.ts           # 模板注册表（来源合并 + 校验 + 冲突覆盖）
│   ├── engine.ts             # 查询执行器
│   ├── adapters/
│   │   ├── balance.ts        # 余额类平台
│   │   ├── subscription.ts   # 官方订阅额度
│   │   ├── coding-plan.ts    # 包月 Coding Plan
│   │   ├── opencode-go.ts    # 首个"内置声明式"参考适配器
│   │   └── script.ts         # 声明式模板执行器（request + extract）
│   ├── credentials.ts        # 凭证解析链
│   ├── cache.ts              # 快照 + 诊断快照环
│   ├── mask.ts               # key 掩码
│   ├── routes.ts             # /api/provider-usage/* 回环路由
│   └── i18n.ts               # zh/en 文案（host 错误消息）
├── src/client/               # Client 半区（TS/React）
│   ├── index.ts              # apply：slots 注入
│   ├── ProviderUsagePanel.tsx
│   ├── ProviderCard.tsx      # 状态圆点 + 套餐卡片组 + 刷新/编辑
│   ├── UsageCard.tsx         # 单套餐进度条（total/used/remaining/resetsAt）
│   ├── TemplateEditor.tsx    # 声明式模板编辑（校验 + 试运行）
│   ├── UsageBadge.tsx        # 侧边栏紧凑徽标
│   ├── locales.ts
│   └── styles.ts / module.css
├── tests/                    # vitest：registry/engine/adapters/extract/安全
└── README.md / README.zh.md
```

构建产物经 `exports`：
- `.` → host（lib/index.js）
- `./client` → browser bundle（`window.__ModuleLoader__.load` 形态，或按 dsh-pet 的 tsdown 双包方式）

---

## 4. 数据模型

### 4.1 查询结果（对齐 cc-switch 的 UsageResult，多套餐）

```ts
interface UsageCard {
  planName?: string;        // 套餐名（余额场景可为 "账户余额"）
  total?: number;           // 总额度
  used?: number;            // 已用
  remaining?: number;       // 剩余
  unit?: string;            // "USD" | "CNY" | "%" | "token" | "次"
  isValid?: boolean;        // 套餐/订阅是否有效
  invalidMessage?: string;  // 失效原因（订阅过期等）
  resetsAt?: string;        // 窗口重置时间（ISO，Go/订阅类）
  extra?: string;           // 自由文本/JSON（如 ZenMux USD 额度明细）
}

interface ProviderUsageResult {
  ok: boolean;
  providerId: string;
  providerName?: string;
  error?: { type: "no-credential" | "unauthorized" | "http" | "parse" | "script" | "network"; message: string; httpStatus?: number };
  queriedAt: number | null;     // 最近成功查询时间
  credential?: { source: string; keyHint: string | null };  // 掩码来源
  cards: UsageCard[];           // 支持多套餐
  snapshots: RequestSnapshot[]; // 诊断（最近 3 次，截断 body）
  stale?: boolean;              // 本次失败、复用上次成功数据
}
```

### 4.2 查询模板（注册表条目）

```ts
interface QueryTemplate {
  id: string;                 // 小写 kebab，如 "deepseek-balance"
  displayName: string;        // "DeepSeek 余额"
  providerIds?: string[];     // 可选：绑定的 DSH provider id（如 "opencode-go"）
  kind: "adapter" | "declarative";   // 内置适配器 | 声明式模板
  adapter?: BuiltinAdapterId; // kind=adapter 时的实现键
  script?: DeclarativeScript; // kind=declarative 时的声明
  source: "builtin" | "user" | "injected";
  version: number;
}

interface DeclarativeScript {
  request: {
    url: string;              // 支持 {{baseUrl}}/{{apiKey}}... 变量
    method: "GET" | "POST" | ...;
    headers: Record<string, string>;
    body?: string;
    timeoutMs?: number;       // 默认 15000，上限 30000
  };
  extract: {                  // 声明式提取（白名单 DSL，见 §6.3）
    mode: "json-path" | "map";
    cards: JsonPathSpec[];
  };
}
```

---

## 5. 查询引擎（CC-switch 风格分派）

### 5.1 模板类型分派

| kind / 场景 | 说明 | 一期/二期 |
|---|---|---|
| `balance`（余额） | DeepSeek / OpenRouter / SiliconFlow / StepFun / Novita 等 | ✅ 一期 |
| `official-subscription`（订阅额度） | Claude（OAuth usage）/ Codex（wham/usage）/ Gemini（OAuth） | ⚠️ 二期（OAuth 托管复杂） |
| `opencode-go`（官方 usage） | `GET https://opencode.ai/zen/go/v1/usage` | ✅ 一期（内置适配器） |
| `coding-plan`（包月套餐） | 火山方舟（AK/SK 签名）/ 智谱 / Kimi Coding / ZenMux | ⚠️ 二期（签名算法多） |
| `copilot`（GitHub Copilot） | Copilot 配额 | ⚠️ 二期（需 OAuth 托管账号） |
| `declarative`（声明式模板） | 任意供应商，用户 JSON 配置 | ✅ 一期 |

> 一期原则：**凡是用 "API Key + 一个 GET" 能查的，一期全做**；
> 需要 OAuth 托管/签名算法的（Claude 订阅、Codex、Gemini、方舟 AK/SK），二期按适配器逐个加。

### 5.2 一期内置适配器端点（实现时逐个实测）

| 适配器 | 方法/URL | 认证 | 结果映射 |
|---|---|---|---|
| deepseek-balance | `GET https://api.deepseek.com/user/balance` | `Authorization: Bearer <key>` | balance_infos → 总余额/可用余额（CNY） |
| openrouter-credits | `GET https://openrouter.ai/api/v1/credits` | Bearer | credits.remaining/total/used（USD） |
| siliconflow | `GET https://api.siliconflow.cn/v1/user/info` | Bearer | balance（CNY） |
| stepfun | `GET https://api.stepfun.com/v1/accounts` | Bearer | balance（CNY） |
| novita | `GET https://api.novita.ai/v3/user/balance` | Bearer | balance（USD） |
| opencode-go | `GET https://opencode.ai/zen/go/v1/usage` | Bearer | usage.{rolling,weekly,monthly} → 3 张卡片（%，resetsAt） |

> 端点清单参考自 cc-switch 源码（balance.rs/coding_plan.rs/subscription.rs）与
> dsh-opencode-go-box，**均为公开接口或已有人验证的端点；实现阶段拿真实 key 逐个验证，
> 任何失效端点降级为"可编辑声明式模板"而不是写死崩溃**。

### 5.3 执行流程

```
engine.query(providerConfig)
  1. 解析模板（registry.resolve(id)）
  2. 解析凭证（credentials.ts 解析链，见 §7）
  3. 语法分派：
     - adapter → adapters/<type>.query(config, creds)
     - declarative → script.ts：变量替换 → URL 安全校验 → fetch → extract 提取
  4. 结果规范化 → ProviderUsageResult
  5. 写缓存（成功/确定性失败） + 诊断快照环 + emit 事件
  6. 浏览器侧 react-query/自研订阅刷新
```

失败语义（对齐 cc-switch）：
- **确定性失败**（401 / 403 / 非 2xx / 解析失败）→ `ok:false` 带类型与 message，写入缓存；
- **瞬时失败**（网络/超时）→ 保留上次成功快照并标 `stale:true`，前端显示旧数据 + 「未更新」。

工程细节：
- 超时 15s（可配，上限 30s）；网络错误重试 1 次（幂等 GET 场景）；
- 多供应商并行查询用有界并发池（对齐 box 的 `mapPool`，默认 4）；
- 轮询默认 60s（平衡第三方接口压力与时效），可配 30s/1m/5m/手动。

---

## 6. 安全设计（重点）

1. **路由仅回环**：`/api/provider-usage/*` 校验 `req.socket.remoteAddress` 与 Host 头，
   非 loopback 一律 403（直接复用 dsh-pet/dsh-balance 的防护模式）。
2. **凭证不落副本**：优先走 DSH 凭证层（`OPENCODE_GO_API_KEY` 等 provider 声明引用）；
   用户模板里需要独立 key 的，存 `$DSH_HOME/provider-usage/`（0600），展示一律掩码
   （前 4 + … + 后 4）。
3. **请求目标白名单**：
   - 内置适配器：URL 写死于代码；
   - 声明式模板：强制 HTTPS、禁止 localhost/内网段（SSRF 防护）、可选 baseUrl 同源校验。
4. **响应体上限**：16 KiB 保留、超长截断；诊断快照 body ≤ 4 KiB。
5. **模板校验 fail-closed**：未知字段/非法 method/非 HTTPS 一律拒载并给诊断
   （对齐 dsh-pet manifest-v2 的纪律）。
6. **不做任意 JS 执行**（一期）：DSH 插件进程拥有用户凭证，加载用户 JS 风险不可控。

### 6.4（二期可选）JS 脚本沙箱对比

| 方案 | 优点 | 缺点 | 结论（二期评） |
|---|---|---|---|
| `node:vm` | 零依赖 | 逃逸面大、资源限制弱 | ❌ 不推荐 |
| QuickJS（quickjs-emscripten） | 内存/超时可限（cc-switch 同款） | 依赖大、wasm 打包折腾 | ⚠️ 备选 |
| **声明式 DSL**（json-path） | 零执行、天然安全、可 Schema 校验 | 表达能力有限 | ✅ 一期先用它 |

---

## 7. 凭证解析链（per provider）

对每个查询，按顺序取第一个可用者：

1. 模板自带 `script.request.headers` 里的固定值/变量（用户显式配置，存用户模板文件）；
2. DSH 设置 → `llm-pi-ai.providers.<id>.apiKeyEnv` 声明的凭证引用（动态发现，对齐 box）；
3. DSH 凭证层常规引用（如 `OPENCODE_GO_API_KEY`、`OPENCODER_API_KEY`、`DEEPSEEK_API_KEY`）；
4. 进程环境变量。

返回 `{ key, source }`，`source` 进诊断展示（如 `credential:OPENCODE_GO_API_KEY`）。

---

## 8. Host API（路由面）

| 路由 | 方法 | 说明 |
|---|---|---|
| `/api/provider-usage/templates` | GET | 注册表清单（内置 + 用户，含校验诊断） |
| `/api/provider-usage/query/:templateId` | GET | 单模板查询（并发去重） |
| `/api/provider-usage/query-all` | GET | 全部已启用模板并行查询 |
| `/api/provider-usage/state` | GET | 当前快照 + 最近查询时间 + stale 标记 |
| `/api/provider-usage/template` | PUT/DELETE | 保存/删除用户模板（校验后落 `$DSH_HOME/provider-usage/`） |
| `/api/provider-usage/test` | POST | 模板试运行（不保存，仅验证 + 返回结果） |

全部走同源 fetch，无 CORS；请求体上限 16 KiB。

---

## 9. Client UI

### 9.1 设置分区（`settings.section` id `provider-usage`，order 40）

- **供应商卡片列表**：名称、状态圆点（🟢 正常 / 🟡 低余量<30% / 🔴 失败或无效 / ⚪ 未配置）、
  最后成功查询时间、卡片组（每个套餐一条进度条 + 重置倒计时）、[刷新] [编辑模板] [诊断展开]；
- **模板管理**：内置模板只读（可复制为用户模板修改）；用户模板可编辑/试运行/删除；
- **诊断**：HTTP 状态、解析版本、凭证来源（掩码）、最近 3 次请求快照（对齐 box 的诊断区块）。

### 9.2 常驻挂件（可选，二期）

- `sidebar.footer.action` 紧凑徽标：`DeepSeek ¥4.9 · Go 45%`，点击展开；
- 或 `conversation.composer.dock` 底部一行（对齐 box 的 dock 挂件）。
- 阈值配色：余额/剩余 >50% 绿、20–50% 黄、<20% 红、rate-limited 红（对齐 dsh-balance 语义）。

### 9.3 i18n

`zh` / `en` 双语文案，经 `dsh-client-locale` 注册 namespace `settings.providerUsage`。

---

## 10. 配置（cordis.patch.yml 插件行）

```yaml
- insert:
    - id: provider-usage
      name: dsh-provider-usage
      config:
        pollMs: 60000            # 轮询间隔（默认 60s，下限 15000）
        timeoutMs: 15000         # 单请求超时（上限 30000）
        warnPercent: 30          # 低余量告警阈值（剩余%）
        maxConcurrency: 4        # 并行查询上限
        maxCardsPerResult: 8     # 单供应商卡片数上限（防异常数据撑爆 UI）
```

用户模板位于 `$DSH_HOME/provider-usage/templates/*.json`（0600），
与内置模板按 `id` 覆盖合并（用户 > 注入 > 内置，对齐 pet 注册表优先级）。

---

## 11. 测试策略

| 层 | 内容 |
|---|---|
| unit（vitest） | registry 合并/冲突/校验、extract DSL、凭证解析链、掩码、结果规范化、失败语义 |
| adapter | 用 mock fetch（vitest + msw）测试各适配器解析（含 401/坏 JSON/旧式 used/limit 兼容） |
| 安全 | URL 校验（HTTP/内网拒绝）、模板 fail-closed、回环 403、body 截断 |
| 契约 | `contracts/*.schema.json` 校验示例模板 |
| e2e（二期） | playwright 挂载 settings.section 与徽标 |

---

## 12. 里程碑

| 里程碑 | 内容 | 交付 |
|---|---|---|
| **M1** | 脚手架（tsdown 双产物、patch、i18n）、registry、声明式模板引擎、routes、设置页骨架、deepseek/openrouter/opencode-go 三个内置适配器 | 可查 3 个供应商余额/Go 窗口 |
| **M2** | 全部余量内置适配器（siliconflow/stepfun/novita）+ 模板管理 UI + 试运行 + 诊断块 + 轮询/缓存/stale | 完整"查询"体验 |
| **M3** | 官方订阅适配器（Claude/Codex/Gemini OAuth 若可行）+ Copilot；dock/侧边栏徽标 | 对齐 cc-switch 查询面 |
| **M4** | （可选）QuickJS 沙箱脚本 + 社区模板库 + 成本统计（DSH 会话日志口径） | 对齐 cc-switch 全量 |

---

## 13. 风险与开放问题

| 风险 | 说明 | 对策 |
|---|---|---|
| 第三方接口未公开/漂移 | usage 类接口无文档、可能变更 | 模板化 + 诊断块 + 过期降级为可编辑模板 |
| 凭证类型多样 | 有些平台不是简单 Bearer（AK/SK 签名、OAuth）| 内置适配器逐个实现，声明式模板先覆盖简单 Bearer 场景 |
| 任意脚本诉求 vs 安全 | 用户想要 cc-switch 的 JS 脚本 | 一期明确不做；用声明式 DSL 覆盖 90% 场景，二期再评沙箱 |
| 与 box 插件重叠 | 已有 opencode-go 专用插件 | 本插件定位通用；opencode-go 做成内置适配器，二者可不冲突共存 |
| OAuth 托管依赖 | Claude/Codex/Gemini 订阅查询需要托管 token | M3 单独评审，不阻塞 M1/M2 |

**开放问题（评审时确认）**：
1. 服务范围：只做"查询"还是顺带做供应商余额低余量提醒（通知）？
2. 挂件形态：侧边栏徽标 / composer dock / 都做？
3. 插件命名：`dsh-provider-usage` 还是 `dsh-cc-usage`（有 cc-switch 影子）？

---

## 14. 参考借鉴

- `dsh-pet`（@linxin666/dsh-pet）：插件形态、注册表、回环路由、测试工程
- `dsh-opencode-go-box`：诊断快照环、stale 语义、凭证解析链、Typert/或本方案自选
- `dsh-balance`（@linshule/dsh-balance）：自建回环路由 + 状态圆点 + 拖拽徽标
- `cc-switch`（farion1231/cc-switch）：模板类型分派、套餐卡片、失败语义、脚本沙箱思路

> 待评审通过后进入 M1 脚手架实现。

---

## 15. M1 落地说明（2026-08-23）

M1 已按本方案实现为**最简版**，与全文的差异如下：

- 范围收敛：仅内置 **OpenCode Go** 一个供应商，无 registry/模板引擎/设置页/徽标挂件；
- 交互：右下角悬浮鲸鱼 🐳（`shell.overlay`），点击弹出「OpenCode Go 用量」对话框（对齐 dsh-pet 的点开弹出交互）；
- 数据：点击时查询 + Host 30s 缓存复用 + 面板打开期间 30s 自动刷新（对齐 cc-switch 按需语义）；
- 解析语义：与用户提供的 cc-switch 脚本一致（percent 钳制、remaining=100-used、extra 倒计时文案）；
- 代码形态：无构建链的手写 ESM（`lib/index.js` + `lib/client.js`），轻于 dsh-pet 的 TS/tsdown 工程；
- 安全：回环 403 防护、凭证走 DSH 凭证层、key 掩码、诊断快照环（3 条）、stale 保留旧值。

文件清单：
- `package.json` / `cordis.patch.yml` — 插件声明与 bundle patch
- `lib/index.js` — Host：查询引擎 + 缓存 + 路由
- `lib/client.js` — Client：悬浮鲸鱼 + 对话框
- `docs/opencode-go-ccswitch-script.md` — 参考脚本原文

后续（M2+）：设置页、多供应商注册表、声明式模板、i18n、侧边栏徽标等，见 §12 里程碑。


---

## 16. M2 落地说明（2026-08-24，v0.4.0）

M2 按"简易版"落地，与全文差异与演进如下：

- **交互重构（用户决策）**：**删除宠物元素**（鲸鱼精灵/悬浮按钮/点击弹框全部移除），改为右下角**常驻用量卡片**；
  卡片布局借鉴 Rainytoken（CATMIAOZHI/Rainytoken，评估见 `tmp/rainytoken-review.md`）的小组件卡片：
  状态点 + 服务名 + ⇄ 供应商切换 + 更新时间 + 刷新；主体主数值 + 窗口行（标签/已用%/进度条/重置时间）。
- **多供应商简易版（用户拍板）**：预设注册表内置 2 个供应商 ——
  `opencode-go`（订阅型 usage-percent，沿用官方 `/zen/go/v1/usage`）+
  `deepseek-balance`（余额型 balance-json，官方 `api.deepseek.com/user/balance`，balance_infos 每币种一卡）。
- **opencode-zen 放弃（调研实录）**：curl 实测（含真实 key）确认 zen 无 Bearer 余额 API（`/zen/user/balance` 等均 404 + SPA），
  GitHub #10448 仍 open；改为 DeepSeek。详见 `tmp/cc-switch-integration-plan.md` §7/§8。
- **路由**：`/api/provider-usage/opencode-go` 兼容保留；新增 `query?provider=` 与 `templates`。
- **凭证链**：`resolveCredentialFor(preset)` 泛化（provider.apiKeyEnv → credentials → env）。
- **缓存**：per-provider 30s 新鲜窗口独立；修复 cordis baseUrl 误覆盖 deepseek 的 404。
- **设置面板**：与官方「模型」页 1:1 的提供方实例清单（从 DSH 导入 / 自定义、增删改、状态圆点）；
  卡片默认右下、**可拖动、位置记忆**。settingsVersion 4→5 无感迁移（兼容 v3/v4）。
- **资产**：删除 `assets/whale-refined/`、`ASSETS-NOTICE.md` 及 asset 路由。
- **测试**：host 87 断言（原 30 + 多供应商/凭证库/缓存 57）；secure-store 9；smoke 改为 templates 断言。

后续（M3）：i18n、声明式自定义模板（§4.2/§5）、余额低余量提醒（§13 开放问题 1）、挂件形态评估（开放问题 2）。
